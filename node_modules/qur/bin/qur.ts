#! /usr/bin/env node

import Create from "qur/src/commands/create";

require('yargs').command('create [model]', 'Create data model (locally).', (yargs) => {
    yargs.positional('model', {
        describe: 'Data model class to create. Sometimes its referred to as entity.'
    })
}, (argv) => {

    new Create

    // todo create model class
    console.log(__dirname, __filename); // TODO DELETE
    


    console.log(argv.model); // TODO DELETE
})
.argv