import readline = require('readline')

var rl = readline.createInterface({
    input: process.stdin,
    output: process.stdout
});

// todo omelette

export default class Command {

    state = {
        configuration: {
            exists(): Boolean {

                // todo 
                return false
            },
            create() {
                // todo 
            }
        }
    }

    constructor() {
        if (!this.state.configuration.exists()) {
            console.log(
                'Just a second!',
                'Seems that you are using Qur for the first time.',
                'Do you prefer Typescript (recommended) or Javascript workflow?',
            );

            rl.question('Write "ts" or "js" (ts)', (answer) => {
                console.log(answer); // TODO DELETE
                
            })
            // todo 

            console.log('conf file created '); // TODO DELETE


            this.state.configuration.create()
        }
    }
}